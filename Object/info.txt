# field dari object dapat diakses dengan "bracket notation"/"dot notation"

# untuk menghapus field(prop/method) dari object, gunakan: 
- delete obj.field

# looping mengakses semua field object (for-in mengambil key):
// Build a Text
let text = "";
for (let x in person) {
  text += person[x] + " ";
};